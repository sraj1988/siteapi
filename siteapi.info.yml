name: 'Site API'
type: module
description: Provides interface to add Site API key and exposes endpoint for page content type JSON respresentation
core: 8.x
configure: system.site_information_settings
dependencies:
  - rest
  - serialization
